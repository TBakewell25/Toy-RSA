/*
#include <math.h>
int* generate_prime(int bits, int k){
	int count, n, i, a, iterations;
	count = 0;
	while(1){
		n = rand();
		if ((n%2) == 0)
			n++;
		count++;
		iterations = 0;
		for (i = 0; i <= k, i++){
			a = random(1, n-1);
			if pow(a, n-1, n) != 1: //c-ize
				break;
			else
				iterations++;
		}
		if (iterations == k)
			return n;
}

*/
